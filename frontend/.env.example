# 前端 API 基地址。开发环境默认走 Vite 代理（/api → http://localhost:8080）。
# 如需直连后端，可设置为完整地址，例如 https://api.example.com/api
VITE_API_BASE=/api

# 可编辑全局作息时间表的账号 label（逗号分隔，仅 UI 限制，后端以 ADMIN_LABELS 为准）
VITE_ADMIN_LABELS=Tangent0712
